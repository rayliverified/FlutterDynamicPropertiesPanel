name: dynamic_properties_panel_example
description: Demonstrates dynamic_properties_panel controller, schema, and polished controls.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dynamic_properties_panel:
    path: ..
  flutter_lucide: ^1.9.0
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
